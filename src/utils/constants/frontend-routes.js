const ROOT_PATH = '/';
const DASHBOARD = '/dashboard';
const USERS = '/users';
const PROFILE = '/profile';

const AUTH = {
  SIGNIN: 'signin',
  SIGNUP: 'signup',
};

const FRONTEND_ROUTES = {
  DASHBOARD,
  AUTH,
  PROFILE,
  ROOT_PATH,
  USERS,
};

export default FRONTEND_ROUTES;
