const DASHBOARD = '/dashboard';
const USERS = '/users';

const AUTH = {
  SIGNIN: 'signin',
  SIGNUP: 'signup',
};

const FRONTEND_ROUTES = {
  DASHBOARD,
  AUTH,
  USERS,
};

export default FRONTEND_ROUTES;
