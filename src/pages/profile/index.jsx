import { Box, Typography } from '@mui/material';

const Profile = () => {
  return (
    <Box>
      <Typography variant='h4' component='h5'>
        Profile
      </Typography>
    </Box>
  );
};

export default Profile;
